# Configuring:
# install miniconda
# conda env create -f environment.yaml
name: MPRL
channels: !!python/tuple
  - defaults
  - cantera
  - conda-forge
dependencies:
  - cantera
  - gitpython
  - ipython
  - matplotlib
  - nose
  - numpy
  - pandas
  - pip
  - scipy
  - tensorflow-gpu
  - tensorboard
  - toml
  - xlrd
  - pip:
      - -r requirements.txt
      - atari-py==0.2.0
      - stable-baselines[mpi]
      - gpyopt
      - parameter-sherpa
